jQuery(function($){$(document).on('click','.tab-container .tab-title',function(e){e.preventDefault();var $this=$(this);var href=$(this).attr("href");$this.closest('.tab-header').find('.tab-title').removeClass('active');$this.addClass('active');var $container=$this.closest('.tab-container');$container.find('.tab-panel').removeClass('active');$container.find('.tab-panel'+href).addClass('active');});});